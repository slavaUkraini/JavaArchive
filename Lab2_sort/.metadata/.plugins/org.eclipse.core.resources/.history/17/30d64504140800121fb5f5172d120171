
public class Sorting {

	/**
	 * @param args
	 */
	int s;
	public static void main(String[] args) /*throw IOException()*/ {
		
		System.out.println("Hello, Vita!");
		DataInput.getInt();
		System.out.println(s);
	/*	const
		  N = 10;

		var
		  a: Array[1..n] of int; i, j, k, t: int;

		begin
		  write('¬вед≥ть масив: ');
		  for i := 1 to n do
		    readln(A[i]);
		  for i := 2 to n do
		  begin
		    t := A[i];
		    j := 1;
		    while (t > A[j]) do j := j + 1;
		    for (k = i - 1; -- j)
		      A[k + 1] := A[k];
		    A[j] = t;
		  end;
		  for (i = 1; i<n; ++i) writeln(A[i]);
		end.
		*/

	}

}
